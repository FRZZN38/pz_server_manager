# PzManager

Project Zomboid dedicated server manager.

## Configuration

Use `appsettings.json.example` as the base configuration.

The current `PzManager` setup expects:
- `Discord:Token`
- `Discord:PublicChannelId`
- `Discord:AdminChannelId`
- `ProjectZomboid:ServerName`
- `ProjectZomboid:InstallDirectory`
- `ProjectZomboid:ConfigDirectory`
- `ProjectZomboid:PerkLogDirectory`

## Projects

- PzManager.App
- PzManager.Core
- PzManager.Server
- PzManager.Discord
- PzManager.Tests
